<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 边框 -->
    <stroke
        android:width="1dp"
        android:color="@color/dp_red"/>
    <!-- 圆角 -->
    <corners android:radius="150dp"/>
    <!-- 边距 -->
    <solid android:color="@color/dp_red"/>
</shape>